---
import CuriosidadesAdmin from '../../components/CuriosidadesAdmin';
import Layout from '../../layouts/Layout.astro';
import '../../styles/curiosidades-admin.css';
import '../../styles/news.css';

const isDev = import.meta.env.DEV;
const serviceKey = import.meta.env.SUPABASE_SERVICE_ROLE_KEY as string;
const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL as string;
---

<Layout title="Admin - Curiosidades">
	{isDev && serviceKey ? (
		<CuriosidadesAdmin client:load serviceKey={serviceKey} supabaseUrl={supabaseUrl} />
	) : (
		<div style="text-align:center;padding:4rem;color:var(--color-text-muted);">
			<p>Panel no disponible.</p>
		</div>
	)}
</Layout>
